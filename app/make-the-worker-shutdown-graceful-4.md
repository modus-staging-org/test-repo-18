# Make the worker shutdown graceful

The cursor skipped the first item of every page after the first. Adds a regression test that walks three pages.

Change #4 of 6 on branch `pr/20260811-121032-4-make-the-worker-shutdown-graceful`.
