# Extract config loading into its own module

Error strings were inconsistent between handlers, which made them awkward to match on. They now follow one shape.

Change #5 of 6 on branch `pr/20260811-121032-5-extract-config-loading-into-its-own-modu`.
